<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="unitec.administradordetarjeta.Login"
    android:id="@+id/ssss">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageView"
        android:layout_marginBottom="13dp"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/tarjeta" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Escribe tu login"
        android:layout_marginBottom="13dp"
        android:id="@+id/editLogin"
        android:layout_gravity="center_horizontal" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="numberPassword"
        android:ems="10"
        android:layout_marginBottom="13dp"
        android:hint="Escribe tu password"
        android:id="@+id/editPassword"
        android:layout_gravity="center_horizontal" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Autenticar"
        android:background="@color/botonrosaClarito"
        android:textColor="@color/blanco"
        android:id="@+id/botonAutenticar"
        android:layout_gravity="center_horizontal" />
</LinearLayout>
